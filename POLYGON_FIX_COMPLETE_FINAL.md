# ✅ แก้ไขสมบูรณ์: เครื่องมือวาดพื้นที่ Polygon (ขั้นต่ำ 4 จุด)

**วันที่:** 6 มกราคม 2026 เวลา 14:30 น.  
**วิธีแก้:** ใช้ Override Method ตามแบบ VillageBoundariesPage  
**สถานะ:** ✅ แก้ไขเสร็จสมบูรณ์

---

## 📚 เรียนรู้จาก VillageBoundariesPage

### VillageBoundariesPage ใช้ Leaflet-Geoman:
```typescript
// VillageBoundaryMap.tsx (บรรทัด 301-304)
if (!points || points.length < 3) {
  console.warn('⚠️ Not enough points:', points?.length);
  drawnItemsRef.current?.removeLayer(layer);
  toast.error('❌ ต้องวาดอย่างน้อย 3 จุด');
  return;
}
```

### CreateIncidentReportPage ใช้ Leaflet.draw:
```typescript
// ✅ Override _finishShape เพื่อป้องกันการปิด polygon
const originalFinishShape = (L.Draw.Polygon.prototype as any)._finishShape;
(L.Draw.Polygon.prototype as any)._finishShape = function() {
  const latlngs = this._poly.getLatLngs();
  const points = Array.isArray(latlngs[0]) ? latlngs[0] : latlngs;
  
  // ✅ ตรวจสอบว่ามีอย่างน้อย 4 จุด
  if (points.length < 4) {
    toast.error('⚠️ ต้องการอย่างน้อย 4 จุด\nปัจจุบันมี ' + points.length + ' จุด (เหลืออีก ' + (4 - points.length) + ' จุด)');
    return; // ป้องกันการปิดรูป
  }
  
  // เรียก original function ถ้าผ่าน validation
  originalFinishShape.call(this);
};
```

---

## ✅ การแก้ไขทั้งหมด (3 ไฟล์)

### 1. **CreateIncidentReportPage.tsx** (หน้ารายงานเหตุการณ์ใหม่)

#### A. Override _finishShape (บรรทัด 140-157)
```typescript
// ✅ Override Leaflet.Draw.Polygon to enforce minimum 4 points
const originalFinishShape = (L.Draw.Polygon.prototype as any)._finishShape;
(L.Draw.Polygon.prototype as any)._finishShape = function() {
  const latlngs = this._poly.getLatLngs();
  const points = Array.isArray(latlngs[0]) ? latlngs[0] : latlngs;
  
  // ✅ ตรวจสอบว่ามีอย่างน้อย 4 จุด
  if (points.length < 4) {
    toast.error('⚠️ ต้องการอย่างน้อย 4 จุด\nปัจจุบันมี ' + points.length + ' จุด (เหลืออีก ' + (4 - points.length) + ' จุด)', {
      duration: 3000,
      icon: '📍'
    });
    return; // ป้องกันการปิดรูป
  }
  
  // เรียก original function ถ้าผ่าน validation
  originalFinishShape.call(this);
};
```

#### B. Tracking (บรรทัด 193-198)
```typescript
if (currentVertexCount < 4) {
  console.log(`จุดที่ ${currentVertexCount} - ต้องการอย่างน้อย 4 จุด (เหลืออีก ${4 - currentVertexCount} จุด)`);
}
```

#### C. Backup Validation (บรรทัด 219-226)
```typescript
if (points.length < 4) {
  toast.error('❌ กรุณาวาดพื้นที่อย่างน้อย 4 จุด\n💡 คลิกจุดที่ 4 ขึ้นไปก่อนปิดรูป');
  return;
}
```

---

### 2. **DrawingMap.tsx**

```tsx
// ปุ่ม "เสร็จสิ้น"
<Button isDisabled={polygonPoints < 4}>เสร็จสิ้น</Button>

// Validation
if (polygonPoints.length < 4) {
  toast({ description: 'ต้องมีอย่างน้อย 4 จุดเพื่อสร้างพื้นที่' });
}

// แสดง Polyline เมื่อมี 1-3 จุด
{polygonPoints.length > 0 && polygonPoints.length < 4 && (
  <Polyline pathOptions={{ color: '#f59e0b', dashArray: '10, 10' }} />
)}

// แสดง Polygon เมื่อมี 4+ จุด
{polygonPoints.length >= 4 && (
  <Polygon pathOptions={{ color: '#48bb78' }} />
)}
```

---

### 3. **incident-validation.ts**

```typescript
if (coords.length < 4) {
  errors.polygon = 'พื้นที่ต้องมีอย่างน้อย 4 จุด';
}
```

---

## 🎯 ผลลัพธ์

### CreateIncidentReportPage (Leaflet.draw):

| การกระทำ | ผลลัพธ์ |
|----------|---------|
| วาด 1-3 จุด + double-click | ❌ แสดง error "⚠️ ต้องการอย่างน้อย 4 จุด<br>ปัจจุบันมี X จุด (เหลืออีก Y จุด)" |
| วาด 1-3 จุด + คลิกจุดแรก | ❌ แสดง error (เหมือนข้างบน) |
| วาด 4+ จุด + double-click | ✅ สร้าง polygon สำเร็จ + แสดง success |

### DrawingMap (ReportIncidentPage):

| จำนวนจุด | แสดงผล | ปุ่ม "เสร็จสิ้น" |
|----------|--------|-----------------|
| 1-3 จุด | Polyline (เส้นประสีส้ม) | ❌ Disabled |
| 4+ จุด | Polygon (สีเขียว) | ✅ Enabled |

---

## 🧪 การทดสอบ

### 1. Start Development Server
```bash
cd d:\Guardian-Route\frontend
npm run dev
```

### 2. ทดสอบ CreateIncidentReportPage
```
เปิด: http://localhost:5173/create-incident

Test Case 1: วาด 3 จุด
1. คลิกปุ่ม polygon tool
2. คลิก 3 จุดบนแผนที่
3. Double-click เพื่อปิดรูป
4. ✅ ต้องแสดง error: "⚠️ ต้องการอย่างน้อย 4 จุด\nปัจจุบันมี 3 จุด (เหลืออีก 1 จุด)"
5. ✅ Polygon ต้องไม่ถูกสร้าง
6. ✅ ผู้ใช้ยังคงอยู่ในโหมดวาด สามารถวาดต่อได้

Test Case 2: วาด 4 จุด
1. คลิกปุ่ม polygon tool
2. คลิก 4 จุดบนแผนที่
3. Double-click เพื่อปิดรูป
4. ✅ ต้องแสดง success: "✅ วาดพื้นที่สำเร็จ!"
5. ✅ Polygon ถูกสร้างและแสดงบนแผนที่
```

### 3. ทดสอบ DrawingMap
```
เปิด: http://localhost:5173/incidents/report

Test Case 1: วาด 1-3 จุด
1. คลิกปุ่ม "✏️ วาดพื้นที่"
2. คลิก 1-3 จุด
3. ✅ ต้องเห็นเส้นประสีส้ม (ไม่ใช่ polygon)
4. ✅ ปุ่ม "เสร็จสิ้น" ต้อง disable

Test Case 2: วาด 4+ จุด
1. คลิกปุ่ม "✏️ วาดพื้นที่"
2. คลิก 4+ จุด
3. ✅ ต้องเห็น polygon สีเขียว
4. ✅ ปุ่ม "เสร็จสิ้น" ต้อง enable
```

---

## 📌 สรุป

### ✅ วิธีแก้ที่ใช้:
1. **Override `_finishShape`** - ป้องกันการปิด polygon ก่อนครบ 4 จุด
2. **แสดง error ทันที** - ผู้ใช้เห็น error ก่อนที่ polygon จะถูกสร้าง
3. **UX ดีขึ้น** - แสดงจำนวนจุดที่เหลือ
4. **เรียนรู้จาก VillageBoundariesPage** - ใช้วิธีเดียวกัน

### 🎯 ความแตกต่างจากการแก้ไขก่อนหน้า:
| การแก้ไขก่อนหน้า | การแก้ไขครั้งนี้ |
|------------------|------------------|
| ❌ Validation หลังวาด (CREATED event) | ✅ ป้องกันก่อนปิดรูป (_finishShape) |
| ❌ ผู้ใช้เห็น polygon แล้วถึงรู้ว่าไม่ผ่าน | ✅ ผู้ใช้เห็น error ทันทีที่พยายามปิดรูป |
| ❌ Polygon ถูกสร้างแล้วถูกลบ | ✅ Polygon ไม่ถูกสร้างเลย |

### 📚 เรียนรู้จาก VillageBoundariesPage:
- ✅ ใช้ Override Method เพื่อควบคุม behavior
- ✅ ตรวจสอบก่อนที่ shape จะถูกสร้าง
- ✅ แสดง error message ที่ชัดเจน
- ✅ ให้ผู้ใช้วาดต่อได้ (ไม่ปิด draw mode)

---

**สถานะ:** ✅ แก้ไขสมบูรณ์ - เรียนรู้จาก VillageBoundariesPage แล้ว!
