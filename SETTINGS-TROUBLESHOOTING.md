# 🔧 Settings Page - Troubleshooting Guide

**ปัญหา:** หน้า Settings ไม่เปลี่ยนแปลง ยังเหมือนเดิม  
**เวลา:** 13:54 น.  
**สาเหตุ:** Frontend server ไม่ได้ hot reload ไฟล์ใหม่

---

## ✅ การแก้ไขที่ทำแล้ว

### 1. Restart Frontend Server ✅
- **ปัญหา:** Frontend server (PID 3464) รันตั้งแต่ 10:00 น. (ก่อนแก้ไข)
- **แก้ไข:** Stop และ restart server
- **ผลลัพธ์:** Server รีสตาร์ทแล้วที่ 13:55 น.

---

## 🔍 วิธีตรวจสอบว่าไฟล์ถูกสร้างแล้ว

### Frontend Files ✅
```
d:\Guardian-Route\frontend\src\
├── pages\admin\SettingsPage.tsx     ← 797 lines ✅
├── pages\admin\SettingsPage.css     ← 515 lines ✅
└── services\settingsService.ts      ← 141 lines ✅
```

### Backend Files ✅
```
d:\Guardian-Route\backend\src\
└── settings\
    ├── dto\update-settings.dto.ts   ✅
    ├── settings.controller.ts       ✅
    ├── settings.service.ts          ✅
    └── settings.module.ts           ✅
```

### Database ✅
```
Migration: 20251117062442_add_settings_table ✅
Table: settings (23 fields) ✅
```

---

## 🚀 ขั้นตอนการทดสอบใหม่

### 1. Hard Refresh Browser
**Windows:**
- Chrome/Edge: `Ctrl + Shift + R` หรือ `Ctrl + F5`
- Firefox: `Ctrl + Shift + R`

**Mac:**
- Chrome/Edge: `Cmd + Shift + R`
- Firefox: `Cmd + Shift + R`

### 2. Clear Browser Cache
1. เปิด DevTools (`F12`)
2. Right-click บน Refresh button
3. เลือก "Empty Cache and Hard Reload"

### 3. ตรวจสอบ Console
1. เปิด DevTools (`F12`)
2. ไปที่ tab "Console"
3. ดูว่ามี error หรือไม่

### 4. ตรวจสอบ Network
1. เปิด DevTools (`F12`)
2. ไปที่ tab "Network"
3. Refresh page
4. ดูว่า `SettingsPage.tsx` โหลดหรือไม่

---

## 📋 Checklist การทดสอบ

### ก่อนทดสอบ
- [x] Frontend server รีสตาร์ทแล้ว (13:55 น.)
- [x] Backend server รันอยู่
- [x] Database migration สำเร็จ
- [x] ไฟล์ทั้งหมดถูกสร้างแล้ว

### ขั้นตอนทดสอบ
1. [ ] Hard refresh browser (`Ctrl + Shift + R`)
2. [ ] Login: `admin@obtwiang.go.th` / `password123`
3. [ ] ไปที่ Settings page: `http://localhost:5173/admin/settings`
4. [ ] ตรวจสอบว่ามี 6 tabs หรือไม่
5. [ ] ตรวจสอบว่า UI เปลี่ยนแปลงหรือไม่

---

## 🎯 สิ่งที่ควรเห็น (Settings Page ใหม่)

### Sidebar Navigation (ซ้าย)
```
หมวดหมู่
⚙️ ทั่วไป
🔒 ผู้ใช้และความปลอดภัย
🗺️ แผนที่และภูมิสารสนเทศ
🔔 การแจ้งเตือน
🔌 การเชื่อมต่อและ API
💾 ข้อมูลและพื้นที่จัดเก็บ
```

### Tab 1: ทั่วไป (General)
```
⚙️ การตั้งค่าทั่วไป
กำหนดค่าพื้นฐานของแอปพลิเคชัน

📝 ชื่อแอปพลิเคชัน
[Guardian Route]

🌍 เขตเวลา
[Dropdown: Bangkok/Singapore/UTC]

🔧 โหมดบำรุงรักษา
[Toggle Switch]

💾 บันทึกการตั้งค่า  ❌ ยกเลิก
```

### Tab 2: ผู้ใช้และความปลอดภัย
```
🔒 ผู้ใช้และความปลอดภัย
กำหนดนโยบายความปลอดภัยในการเข้าถึง

🔐 บังคับใช้ Two-Factor Authentication
[Toggle Switch]

🔑 ความยาวรหัสผ่านขั้นต่ำ
[Number Input: 8-32]

⏱️ ระยะเวลาเซสชัน
[Number Input: 5-120 นาที]

🌐 IP Address Allowlist
[Text Area]

💾 บันทึกการตั้งค่า  ❌ ยกเลิก
```

### Tab 3: แผนที่และภูมิสารสนเทศ
```
🗺️ แผนที่และภูมิสารสนเทศ
ปรับแต่งการแสดงผลแผนที่

📍 Default Latitude
[19.9167]

📍 Default Longitude
[99.8833]

🔍 Default Zoom Level
[13]

🗺️ Default Base Layer
[Dropdown: Satellite/Street]

🌐 Custom Tile Server URL
[Text Input]

☁️ เปิดใช้งานเรดาร์สภาพอากาศ
[Toggle Switch]

💾 บันทึกการตั้งค่า  ❌ ยกเลิก
```

### Tab 4: การแจ้งเตือน
```
🔔 การแจ้งเตือน
ควบคุมการส่งการแจ้งเตือนอัตโนมัติ

📧 ส่งอีเมลเมื่อมีเหตุการณ์ใหม่
[Toggle Switch]

📱 ส่ง SMS เมื่อมีเหตุการณ์ความรุนแรงสูง
[Toggle Switch]

📊 ส่งสรุปรายวันทางอีเมล
[Toggle Switch]

💬 เปิดใช้งาน LINE Notify
[Toggle Switch]

🔑 LINE Notify Access Token
[Password Input - แสดงเมื่อเปิด LINE Notify]

💾 บันทึกการตั้งค่า  ❌ ยกเลิก
```

### Tab 5: การเชื่อมต่อและ API
```
🔌 การเชื่อมต่อและ API
จัดการ API Keys และ Rate Limiting

☁️ Weather API Key
[Password Input]

📱 SMS Gateway API Key
[Password Input]

⚡ Rate Limiting
จำนวนคำขอสูงสุดต่อนาที: [60]
ระยะเวลาที่บล็อก: [300 วินาที]

💾 บันทึกการตั้งค่า  ❌ ยกเลิก
```

### Tab 6: ข้อมูลและพื้นที่จัดเก็บ
```
💾 ข้อมูลและพื้นที่จัดเก็บ
กำหนดนโยบายการจัดเก็บและสำรองข้อมูล

📅 ระยะเวลาจัดเก็บข้อมูล
[365 วัน]

💾 ความถี่การสำรองข้อมูล
[Dropdown: Daily/Weekly/Monthly/Disabled]

💾 บันทึกการตั้งค่า  ❌ ยกเลิก

⚠️ Danger Zone
การกระทำเหล่านี้มีความเสี่ยงสูง กรุณาใช้ความระมัดระวัง

🗑️ ลบข้อมูลเก่า (Purge Old Data)
[ลบข้อมูลเก่า]

🚨 รีเซ็ตระบบ (Factory Reset)
[Factory Reset]
```

---

## 🔴 ถ้ายังไม่เห็นการเปลี่ยนแปลง

### Option 1: ตรวจสอบ Route
1. เปิด `frontend/src/App.tsx`
2. ตรวจสอบว่ามี route `/admin/settings` หรือไม่
3. ตรวจสอบว่า import SettingsPage ถูกต้องหรือไม่

### Option 2: ตรวจสอบ Import
1. เปิด DevTools Console
2. ดู error message
3. ตรวจสอบว่ามี import error หรือไม่

### Option 3: ตรวจสอบ File Path
```bash
# ตรวจสอบว่าไฟล์อยู่ที่ถูกต้อง
ls frontend/src/pages/admin/SettingsPage.tsx
ls frontend/src/pages/admin/SettingsPage.css
ls frontend/src/services/settingsService.ts
```

### Option 4: Restart ทุกอย่าง
```bash
# Stop ทุก process
# Restart backend
cd backend
npm run start:dev

# Restart frontend (terminal ใหม่)
cd frontend
npm run dev
```

---

## 📸 Screenshot ตัวอย่าง

### หน้าเดิม (ก่อนแก้ไข)
- มีแค่ข้อความ "Settings Page"
- ไม่มี tabs
- ไม่มี form controls

### หน้าใหม่ (หลังแก้ไข)
- มี sidebar navigation (6 tabs)
- มี form controls ครบถ้วน
- มี loading state
- มี save/cancel buttons
- มี danger zone

---

## ✅ การยืนยันว่าระบบทำงาน

### Test 1: Page Load
1. ไปที่ `/admin/settings`
2. ควรเห็น loading spinner
3. ควรเห็น toast: "โหลดการตั้งค่าเรียบร้อย"

### Test 2: Tab Navigation
1. คลิกแต่ละ tab ใน sidebar
2. Content ควรเปลี่ยนตาม tab

### Test 3: Save Settings
1. แก้ไขค่าใน tab ใดก็ได้
2. คลิก "บันทึกการตั้งค่า"
3. ควรเห็น toast: "บันทึกการตั้งค่า...เรียบร้อย"

---

## 🆘 ติดต่อ Support

ถ้ายังมีปัญหา ให้ส่งข้อมูลต่อไปนี้:

1. **Screenshot** หน้า Settings ปัจจุบัน
2. **Console Errors** (F12 → Console tab)
3. **Network Errors** (F12 → Network tab)
4. **Browser Version** (Chrome/Edge/Firefox)
5. **URL ที่เข้า** (ควรเป็น `/admin/settings`)

---

**อัปเดตล่าสุด:** 17 พ.ย. 2568 - 13:55 น.  
**Status:** Frontend server รีสตาร์ทแล้ว ✅  
**Next Step:** Hard refresh browser และทดสอบใหม่
