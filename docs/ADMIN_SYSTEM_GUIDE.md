# คู่มือระบบผู้ดูแลระบบ (Admin System)

**Version:** 2.1.0  
**Last Updated:** November 9, 2025  
**Status:** ✅ Production Ready

---

## 📋 สารบัญ

1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [คุณสมบัติหลัก](#คุณสมบัติหลัก)
3. [การเข้าถึงระบบ](#การเข้าถึงระบบ)
4. [User Management](#user-management)
5. [GeoJSON Management](#geojson-management)
6. [Audit Logs](#audit-logs)
7. [System Settings](#system-settings)
8. [API Endpoints](#api-endpoints)
9. [Security](#security)

---

## ภาพรวมระบบ

ระบบผู้ดูแลระบบ (Admin System) เป็นส่วนหนึ่งของ Guardian Route v2.1.0 ที่ออกแบบมาเพื่อให้ผู้ดูแลระบบสามารถจัดการผู้ใช้งาน ขอบเขตภูมิศาสตร์ การตั้งค่าระบบ และติดตามกิจกรรมต่างๆ ได้อย่างมีประสิทธิภาพ

### เทคโนโลยีที่ใช้

**Backend:**
- NestJS Framework
- Prisma ORM
- PostgreSQL Database
- JWT Authentication
- Role-based Access Control (RBAC)

**Frontend:**
- React + TypeScript
- Chakra UI
- React Query (TanStack Query)
- React Hook Form
- React Dropzone (for file upload)

---

## คุณสมบัติหลัก

### 1. 👤 User Management
- สร้าง แก้ไข และลบผู้ใช้งาน
- เปลี่ยนบทบาท (Role) ของผู้ใช้
- ระงับ/เปิดใช้งานบัญชีผู้ใช้
- ค้นหาและกรองผู้ใช้
- แสดงสถิติผู้ใช้งาน

### 2. 🗺️ GeoJSON Management
- อัพโหลด GeoJSON ผ่าน Drag & Drop
- รองรับทุกประเภท GeoJSON (Point, Polygon, Feature, FeatureCollection, etc.)
- โหมด Merge และ Overwrite
- เชื่อมโยงกับ Village ID
- ดาวน์โหลด GeoJSON
- แสดงบนแผนที่

### 3. 🧾 Audit Logs
- บันทึกการกระทำทั้งหมดของ Admin
- กรองตามการกระทำ ประเภทเป้าหมาย และช่วงเวลา
- Export เป็น CSV
- แสดงรายละเอียด IP Address และ User Agent
- Pagination

### 4. ⚙️ System Settings
- ตั้งค่าแผนที่ (Center, Zoom)
- เปิด/ปิดฟีเจอร์ (Gemini AI, Notifications, Broadcast)
- จัดการ API Keys
- ตั้งค่า Session Timeout
- กำหนดขนาดไฟล์อัพโหลดสูงสุด
- รีเซ็ตเป็นค่าเริ่มต้น

---

## การเข้าถึงระบบ

### สิทธิ์การเข้าถึง

ระบบผู้ดูแลสามารถเข้าถึงได้โดย:
- **Role:** `ADMIN` เท่านั้น
- **Authentication:** JWT Token
- **Protection:** Role-based Guards ทุก Endpoint

### การเข้าสู่ระบบ

1. Login ด้วยบัญชี ADMIN
2. ไปที่เมนู "ระบบผู้ดูแลระบบ" หรือ `/admin`
3. เลือก Tab ที่ต้องการจัดการ

---

## User Management

### การสร้างผู้ใช้ใหม่

**ขั้นตอน:**
1. คลิกปุ่ม "เพิ่มผู้ใช้"
2. กรอกข้อมูล:
   - **Username** (required)
   - **Email** (required)
   - **Password** (required, อย่างน้อย 6 ตัวอักษร)
   - **Full Name** (required)
   - **Role** (required)
   - **Phone** (optional)
   - **Department** (optional)
3. คลิก "สร้างผู้ใช้"

**API Endpoint:**
```
POST /api/admin/users
```

**Request Body:**
```json
{
  "username": "john_doe",
  "email": "john@example.com",
  "password": "password123",
  "fullName": "John Doe",
  "role": "FIELD_OFFICER",
  "phone": "081-234-5678",
  "department": "แผนกป้องกันและบรรเทาสาธารณภัย"
}
```

### การแก้ไขผู้ใช้

**ขั้นตอน:**
1. คลิกปุ่ม "..." ในแถวของผู้ใช้
2. เลือก "แก้ไข"
3. แก้ไขข้อมูลที่ต้องการ
4. คลิก "บันทึก"

**หมายเหตุ:** ถ้าไม่ต้องการเปลี่ยนรหัสผ่าน ให้เว้นช่อง Password ว่างไว้

### การเปลี่ยนบทบาท

**ขั้นตอน:**
1. คลิกปุ่ม "..." ในแถวของผู้ใช้
2. เลือก "เปลี่ยนบทบาท"
3. เลือก Role ใหม่
4. ยืนยันการเปลี่ยนแปลง

**บทบาทที่รองรับ:**
- `ADMIN` - ผู้ดูแลระบบ
- `EXECUTIVE` - ผู้บริหาร
- `SUPERVISOR` - หัวหน้างาน
- `FIELD_OFFICER` - เจ้าหน้าที่ภาคสนาม

### การระงับ/เปิดใช้งานผู้ใช้

**ขั้นตอน:**
1. คลิกปุ่ม "..." ในแถวของผู้ใช้
2. เลือก "ระงับผู้ใช้" หรือ "เปิดใช้งาน"
3. ยืนยันการเปลี่ยนแปลง

**ผลกระทบ:**
- ผู้ใช้ที่ถูกระงับจะไม่สามารถ Login เข้าระบบได้
- ข้อมูลของผู้ใช้จะยังคงอยู่ในระบบ

### การลบผู้ใช้

**ขั้นตอน:**
1. คลิกปุ่ม "..." ในแถวของผู้ใช้
2. เลือก "ลบผู้ใช้"
3. ยืนยันการลบ (ไม่สามารถยกเลิกได้)

**คำเตือน:**
- การลบผู้ใช้จะลบข้อมูลออกจากระบบถาวร
- ไม่สามารถลบตัวเองได้

### การค้นหาและกรอง

**ตัวกรองที่รองรับ:**
- **Search:** ค้นหาจาก Username, Email, Full Name
- **Role:** กรองตามบทบาท
- **Status:** กรองตามสถานะ (ใช้งาน/ระงับ)

---

## GeoJSON Management

### การอัพโหลด GeoJSON

**ขั้นตอน:**
1. ไปที่ Tab "ขอบเขตภูมิศาสตร์"
2. เลือก Sub-tab "อัพโหลด GeoJSON"
3. ลากไฟล์ GeoJSON มาวางในกล่อง หรือคลิกเพื่อเลือกไฟล์
4. กรอกข้อมูล:
   - **ชื่อขอบเขต** (required)
   - **ประเภท** (village, district, province, custom)
   - **Village ID** (optional)
   - **โหมด** (merge/overwrite)
5. คลิก "อัพโหลด GeoJSON"

**ประเภท GeoJSON ที่รองรับ:**
- Point
- LineString
- Polygon
- MultiPoint
- MultiLineString
- MultiPolygon
- GeometryCollection
- Feature
- FeatureCollection

**โหมดการอัพโหลด:**
- **Merge:** เพิ่มขอบเขตใหม่โดยไม่ลบข้อมูลเดิม
- **Overwrite:** ลบขอบเขตเดิมของหมู่บ้านนี้และเขียนทับด้วยข้อมูลใหม่

### การดาวน์โหลด GeoJSON

**ขั้นตอน:**
1. ไปที่ Sub-tab "รายการ GeoJSON"
2. คลิกปุ่ม "..." ในแถวของ GeoJSON ที่ต้องการ
3. เลือก "ดาวน์โหลด"
4. ไฟล์จะถูกดาวน์โหลดในรูปแบบ `.geojson`

### การลบ GeoJSON

**ขั้นตอน:**
1. คลิกปุ่ม "..." ในแถวของ GeoJSON
2. เลือก "ลบ"
3. ยืนยันการลบ

---

## Audit Logs

### การดูบันทึกการตรวจสอบ

**ข้อมูลที่แสดง:**
- เวลาที่เกิดเหตุการณ์
- ผู้ใช้ที่ทำการกระทำ
- การกระทำ (Action)
- เป้าหมาย (Target Type + Target ID)
- IP Address
- รายละเอียดเพิ่มเติม (JSON)

**การกระทำที่บันทึก:**
- `CREATE_USER` - สร้างผู้ใช้
- `UPDATE_USER` - แก้ไขผู้ใช้
- `DELETE_USER` - ลบผู้ใช้
- `SUSPEND_USER` - ระงับผู้ใช้
- `ACTIVATE_USER` - เปิดใช้งานผู้ใช้
- `CHANGE_ROLE` - เปลี่ยนบทบาท
- `UPLOAD_GEOJSON` - อัพโหลด GeoJSON
- `EDIT_POLYGON` - แก้ไข Polygon
- `DELETE_GEOJSON` - ลบ GeoJSON
- `UPDATE_SETTINGS` - อัพเดทการตั้งค่า
- `RESET_SETTINGS` - รีเซ็ตการตั้งค่า

### การกรองบันทึก

**ตัวกรองที่รองรับ:**
- **การกระทำ:** เลือกจาก 11 ประเภท
- **ประเภทเป้าหมาย:** USER, GEOJSON, SETTINGS
- **วันที่เริ่มต้น:** กรองตั้งแต่วันที่
- **วันที่สิ้นสุด:** กรองถึงวันที่

### การ Export CSV

**ขั้นตอน:**
1. (Optional) ตั้งค่าตัวกรอง
2. คลิกปุ่ม "Export CSV"
3. ไฟล์ CSV จะถูกดาวน์โหลด

**รูปแบบ CSV:**
```csv
ID,User ID,Username,Action,Target Type,Target ID,IP Address,Created At
...
```

---

## System Settings

### การตั้งค่าแผนที่

**ตัวเลือก:**
- **ละติจูดกลาง:** ตำแหน่งกลางของแผนที่ (เช่น 18.7883)
- **ลองจิจูดกลาง:** ตำแหน่งกลางของแผนที่ (เช่น 98.9853)
- **ระดับซูมเริ่มต้น:** ระดับซูมเมื่อเปิดแผนที่ (เช่น 13)

### การเปิด/ปิดฟีเจอร์

**ฟีเจอร์ที่สามารถควบคุมได้:**
- **Gemini AI:** เปิด/ปิดการใช้งาน AI สำหรับวิเคราะห์รายงาน
- **การแจ้งเตือน:** เปิด/ปิดระบบแจ้งเตือน
- **Broadcast:** เปิด/ปิดการแจ้งเตือนแบบ Broadcast

### การตั้งค่า API

**API Keys:**
- **Gemini API Key:** API Key สำหรับ Google Gemini AI

**หมายเหตุ:** API Key จะถูกเก็บในรูปแบบ encrypted ในฐานข้อมูล

### การตั้งค่าอื่นๆ

**ตัวเลือก:**
- **Session Timeout:** ระยะเวลาก่อน Session หมดอายุ (นาที)
- **ขนาดไฟล์อัพโหลดสูงสุด:** ขนาดไฟล์สูงสุดที่อนุญาต (MB)

### การรีเซ็ตการตั้งค่า

**ขั้นตอน:**
1. คลิกปุ่ม "รีเซ็ตเป็นค่าเริ่มต้น"
2. ยืนยันการรีเซ็ต
3. การตั้งค่าทั้งหมดจะถูกรีเซ็ตเป็นค่าเริ่มต้น

**ค่าเริ่มต้น:**
```json
{
  "mapConfig": {
    "centerLat": 18.7883,
    "centerLng": 98.9853,
    "defaultZoom": 13
  },
  "geminiAiEnabled": true,
  "notificationsEnabled": true,
  "broadcastEnabled": true,
  "systemName": "Guardian Route",
  "sessionTimeout": 60,
  "maxUploadSize": 10
}
```

---

## API Endpoints

### User Management

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/admin/users` | สร้างผู้ใช้ใหม่ |
| GET | `/api/admin/users` | ดึงรายการผู้ใช้ |
| GET | `/api/admin/users/stats` | ดึงสถิติผู้ใช้ |
| GET | `/api/admin/users/:id` | ดึงข้อมูลผู้ใช้ตาม ID |
| PATCH | `/api/admin/users/:id` | อัพเดทข้อมูลผู้ใช้ |
| PATCH | `/api/admin/users/:id/role` | เปลี่ยน Role ของผู้ใช้ |
| PATCH | `/api/admin/users/:id/toggle-status` | Suspend/Activate ผู้ใช้ |
| DELETE | `/api/admin/users/:id` | ลบผู้ใช้ |

### GeoJSON Management

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/admin/geojson` | อัพโหลด GeoJSON |
| GET | `/api/admin/geojson` | ดึงรายการ GeoJSON |
| GET | `/api/admin/geojson/stats` | ดึงสถิติ GeoJSON |
| GET | `/api/admin/geojson/:id` | ดึง GeoJSON ตาม ID |
| PATCH | `/api/admin/geojson/:id` | อัพเดท GeoJSON |
| DELETE | `/api/admin/geojson/:id` | ลบ GeoJSON |

### Audit Logs

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/admin/audit-logs` | ดึงรายการ Audit Logs |
| GET | `/api/admin/audit-logs/stats` | ดึงสถิติ Audit Logs |
| GET | `/api/admin/audit-logs/:id` | ดึง Audit Log ตาม ID |
| GET | `/api/admin/audit-logs/export/csv` | Export Audit Logs เป็น CSV |

### System Settings

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/admin/settings` | ดึงการตั้งค่าทั้งหมด |
| GET | `/api/admin/settings/:key` | ดึงการตั้งค่าตาม key |
| PATCH | `/api/admin/settings` | อัพเดทการตั้งค่า |
| POST | `/api/admin/settings/reset` | รีเซ็ตเป็นค่าเริ่มต้น |
| DELETE | `/api/admin/settings/:key` | ลบการตั้งค่าตาม key |

---

## Security

### Authentication & Authorization

**การยืนยันตัวตน:**
- ใช้ JWT Token
- Token ต้องมีใน Header: `Authorization: Bearer <token>`

**การตรวจสอบสิทธิ์:**
- ทุก Endpoint ต้องมี Role = `ADMIN`
- ใช้ `@Roles(Role.ADMIN)` Decorator
- ใช้ `RolesGuard` สำหรับตรวจสอบ

### Audit Logging

**ข้อมูลที่บันทึก:**
- User ID และ Username
- การกระทำ (Action)
- เป้าหมาย (Target Type + Target ID)
- รายละเอียด (Details)
- IP Address
- User Agent
- Timestamp

**การป้องกัน:**
- ไม่สามารถแก้ไขหรือลบ Audit Logs ได้
- Audit Logs เป็น append-only

### Data Protection

**Password:**
- Hash ด้วย bcrypt (10 rounds)
- ไม่ส่ง password กลับใน response

**API Keys:**
- เก็บในฐานข้อมูลแบบ encrypted
- แสดงเป็น password field ใน UI

**Validation:**
- ใช้ class-validator ใน DTOs
- ตรวจสอบ GeoJSON format
- ป้องกันการลบตัวเอง
- ป้องกันการเปลี่ยน role ของตัวเอง

---

## Best Practices

### User Management
1. ใช้ username ที่ไม่ซ้ำกัน
2. ใช้ email ที่ถูกต้อง
3. ตั้งรหัสผ่านที่แข็งแรง (อย่างน้อย 6 ตัวอักษร)
4. ระงับผู้ใช้แทนการลบ (ถ้าไม่จำเป็น)
5. ตรวจสอบ Audit Logs เป็นประจำ

### GeoJSON Management
1. ตรวจสอบ GeoJSON format ก่อนอัพโหลด
2. ใช้ชื่อที่สื่อความหมาย
3. เลือกประเภทที่ถูกต้อง
4. ใช้ Merge mode เป็นค่าเริ่มต้น
5. Backup GeoJSON ก่อนใช้ Overwrite mode

### System Settings
1. Backup การตั้งค่าก่อนเปลี่ยนแปลง
2. ทดสอบการตั้งค่าใหม่ใน Staging ก่อน
3. บันทึกค่า API Keys ไว้ในที่ปลอดภัย
4. ตั้งค่า Session Timeout ให้เหมาะสม
5. จำกัดขนาดไฟล์อัพโหลดตามความจำเป็น

---

## Troubleshooting

### ไม่สามารถสร้างผู้ใช้ได้

**สาเหตุที่เป็นไปได้:**
- Username หรือ Email ซ้ำ
- Password สั้นเกินไป (ต้องอย่างน้อย 6 ตัวอักษร)
- ข้อมูลไม่ครบถ้วน

**วิธีแก้:**
- ตรวจสอบว่า username และ email ไม่ซ้ำกับผู้ใช้อื่น
- ตั้งรหัสผ่านที่ยาวพอ
- กรอกข้อมูลที่จำเป็นทั้งหมด

### GeoJSON อัพโหลดไม่สำเร็จ

**สาเหตุที่เป็นไปได้:**
- ไฟล์ไม่ใช่ GeoJSON format ที่ถูกต้อง
- ขนาดไฟล์ใหญ่เกินไป
- JSON syntax ผิด

**วิธีแก้:**
- ตรวจสอบ GeoJSON format ด้วย online validator
- ลดขนาดไฟล์หรือเพิ่ม maxUploadSize
- แก้ไข JSON syntax error

### Audit Logs ไม่แสดงข้อมูล

**สาเหตุที่เป็นไปได้:**
- ตัวกรองเข้มเกินไป
- ยังไม่มีการกระทำใดๆ
- ปัญหาการเชื่อมต่อฐานข้อมูล

**วิธีแก้:**
- ลบตัวกรองทั้งหมด
- ทำการกระทำบางอย่าง (เช่น สร้างผู้ใช้)
- ตรวจสอบการเชื่อมต่อฐานข้อมูล

---

## Support

หากพบปัญหาหรือต้องการความช่วยเหลือ กรุณาติดต่อ:
- **Email:** support@guardianroute.com
- **GitHub Issues:** https://github.com/jetci/Guardian-Route/issues
- **Documentation:** https://docs.guardianroute.com

---

**© 2025 Guardian Route - Admin System v2.1.0**
