# คู่มือการใช้งานระบบขอบเขตพื้นที่ตำบลและหมู่บ้าน
## Guardian Route - Village & Tambon Boundaries Management

---

## 📋 สารบัญ
1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [ฟีเจอร์หลัก](#ฟีเจอร์หลัก)
3. [การใช้งาน](#การใช้งาน)
4. [API Endpoints](#api-endpoints)
5. [โครงสร้างข้อมูล](#โครงสร้างข้อมูล)
6. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## 🎯 ภาพรวมระบบ

ระบบจัดการขอบเขตพื้นที่ช่วยให้ผู้ดูแลระบบสามารถ:
- **กำหนดขอบเขตตำบล** - วาดและจัดเก็บขอบเขตตำบลเวียง อำเภอฝาง
- **กำหนดขอบเขตหมู่บ้าน** - วาดขอบเขตสำหรับหมู่บ้านทั้ง 20 หมู่
- **นำเข้า GeoJSON** - อัปโหลดไฟล์ GeoJSON ที่มีอยู่แล้ว
- **Georeference Tool** - นำภาพแผนที่มาทาบบนแผนที่ดิจิทัล

---

## ✨ ฟีเจอร์หลัก

### 1. การวาดขอบเขตบนแผนที่
- เครื่องมือวาดรูปหลายเหลี่ยม (Polygon)
- เครื่องมือวาดสี่เหลี่ยม (Rectangle)
- เครื่องมือปักหมุด (Marker)
- แก้ไขและลบขอบเขตที่วาดแล้ว

### 2. การจัดการขอบเขตตำบล
- วาดขอบเขตตำบลเวียง
- บันทึกลงฐานข้อมูล PostgreSQL
- แสดงผลบนแผนที่ด้วยสีและสไตล์เฉพาะ
- โหลดจาก API แทนข้อมูล hardcode

### 3. การจัดการขอบเขตหมู่บ้าน
- วาดขอบเขตสำหรับหมู่บ้านแต่ละหมู่ (1-20)
- บันทึกพิกัดกลางหมู่บ้านอัตโนมัติ
- แก้ไขขอบเขตที่มีอยู่
- ลบขอบเขตหมู่บ้าน
- ดูรายการหมู่บ้านที่มีขอบเขตแล้ว

### 4. Georeference Image Tool
- อัปโหลดภาพแผนที่/ภาพถ่ายทางอากาศ
- ปรับความโปร่งใส (Opacity)
- ปรับขนาด (Scale)
- หมุนภาพ (Rotation)
- ลากเพื่อวางตำแหน่ง

### 5. การนำเข้า/ส่งออก GeoJSON
- อัปโหลดไฟล์ .json หรือ .geojson
- ส่งออกขอบเขตทั้งหมดเป็น GeoJSON
- รองรับ Feature, FeatureCollection, Polygon, MultiPolygon

---

## 🚀 การใช้งาน

### เข้าสู่หน้าจัดการขอบเขต
1. เข้าสู่ระบบด้วยบัญชี **Admin** หรือ **Developer**
2. ไปที่เมนู **Admin** → **กำหนดขอบเขตหมู่บ้าน**

### วาดขอบเขตตำบล
1. คลิกปุ่ม **🏛️ แก้ไขขอบเขตตำบล**
2. ใช้เครื่องมือวาดรูปหลายเหลี่ยม (Polygon) บนแผนที่
3. คลิกจุดต่างๆ เพื่อวาดขอบเขต
4. คลิกจุดแรกอีกครั้งเพื่อปิดรูป
5. กรอกชื่อ: **ตำบลเวียง**
6. เลือกประเภท: **🏛️ ขอบเขตตำบลเวียง**
7. คลิก **💾 บันทึกขอบเขต**

### วาดขอบเขตหมู่บ้าน
1. ไปที่แท็บ **🗺️ แผนที่**
2. ใช้เครื่องมือวาดรูปหลายเหลี่ยม
3. วาดขอบเขตหมู่บ้านบนแผนที่
4. กรอกชื่อหมู่บ้าน เช่น **หมู่ 1 - หนองตุ้ม**
5. เลือกหมู่ที่: **หมู่ 1**
6. คลิก **💾 บันทึกขอบเขต**

### แก้ไขขอบเขตหมู่บ้าน
1. ในรายการขอบเขต คลิกปุ่ม **✏️** ที่หมู่บ้านที่ต้องการแก้ไข
2. วาดขอบเขตใหม่บนแผนที่
3. คลิก **✏️ บันทึกการแก้ไข**

### ลบขอบเขตหมู่บ้าน
1. ในรายการขอบเขต คลิกปุ่ม **🗑️** ที่หมู่บ้านที่ต้องการลบ
2. ยืนยันการลบ
3. ขอบเขตจะถูกลบออกจากฐานข้อมูล

### นำเข้า GeoJSON
1. ไปที่แท็บ **📁 อัปโหลด GeoJSON**
2. คลิก **เลือกไฟล์** หรือลากไฟล์มาวาง
3. เลือกไฟล์ .json หรือ .geojson
4. ระบบจะบันทึกอัตโนมัติ

### ส่งออก GeoJSON
1. คลิกปุ่ม **📥 ส่งออก GeoJSON**
2. ไฟล์จะถูกดาวน์โหลดในรูปแบบ `village-boundaries-YYYY-MM-DD.geojson`

### ใช้งาน Georeference Tool
1. ในแท็บแผนที่ มองหาส่วน **🗺️ Georeference Image Tool**
2. คลิก **📷 เลือกภาพ (JPG, PNG)**
3. เลือกภาพแผนที่หรือภาพถ่ายทางอากาศ
4. ปรับค่าต่างๆ:
   - **Opacity**: ความโปร่งใส 0-100%
   - **Scale**: ขนาด 0.1x-5x
   - **Rotation**: การหมุน 0-360°
5. ลากภาพเพื่อวางตำแหน่งบนแผนที่
6. วาดขอบเขตทับภาพที่วางไว้

---

## 🔌 API Endpoints

### Backend API (NestJS)

#### ขอบเขตตำบล
```http
GET    /admin/tambon/boundary          # ดึงข้อมูลขอบเขตตำบล
POST   /admin/tambon/boundary          # บันทึก/อัปเดตขอบเขตตำบล
```

#### ขอบเขตหมู่บ้าน
```http
GET    /admin/villages/boundaries      # ดึงรายการขอบเขตหมู่บ้านทั้งหมด
PUT    /admin/villages/:id/boundary    # อัปเดตขอบเขตหมู่บ้าน
DELETE /admin/villages/:id/boundary    # ลบขอบเขตหมู่บ้าน
```

#### GeoJSON ทั่วไป
```http
GET    /admin/geojson                  # ดึง GeoJSON ทั้งหมด
GET    /admin/geojson/:id              # ดึง GeoJSON ตาม ID
POST   /admin/geojson/upload           # อัปโหลด GeoJSON
POST   /admin/boundaries/draw          # บันทึกขอบเขตที่วาด
PUT    /admin/geojson/:id              # อัปเดต GeoJSON
DELETE /admin/geojson/:id              # ลบ GeoJSON
```

### Frontend Service

```typescript
// boundariesService.ts
import boundariesService from '@/services/boundariesService';

// ดึงขอบเขตตำบล
const tambon = await boundariesService.getTambonBoundary();

// บันทึกขอบเขตตำบล
await boundariesService.saveTambonBoundary({
  name: 'ตำบลเวียง',
  geojson: geojsonData,
  properties: { district: 'อำเภอฝาง', province: 'จังหวัดเชียงใหม่' }
});

// ดึงขอบเขตหมู่บ้าน
const villages = await boundariesService.getVillageBoundaries();

// อัปเดตขอบเขตหมู่บ้าน
await boundariesService.updateVillageBoundary(villageId, boundary, centerPoint);

// ลบขอบเขตหมู่บ้าน
await boundariesService.deleteVillageBoundary(villageId);
```

---

## 📊 โครงสร้างข้อมูล

### Database Schema (Prisma)

#### GeoBoundary Model
```prisma
model GeoBoundary {
  id         String   @id @default(uuid())
  name       String
  type       String   // 'tambon', 'village', 'district', 'province', 'custom'
  geojson    Json     // GeoJSON object
  properties Json?    // Additional properties
  villageId  String?  // Optional link to village
  uploadedBy String
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}
```

#### Village Model
```prisma
model Village {
  id             String   @id @default(uuid())
  villageNo      Int      @unique
  name           String
  centerPoint    Json?    // GeoJSON Point
  boundary       Json?    // GeoJSON Polygon
  households     Int?
  population     Int?
  area           Decimal?
  // ... other fields
}
```

### GeoJSON Format

#### Tambon Boundary
```json
{
  "type": "Feature",
  "properties": {
    "name": "ตำบลเวียง",
    "type": "tambon",
    "district": "อำเภอฝาง",
    "province": "จังหวัดเชียงใหม่",
    "area": 120.5,
    "population": 9000,
    "villages": 20
  },
  "geometry": {
    "type": "Polygon",
    "coordinates": [[[lng, lat], [lng, lat], ...]]
  }
}
```

#### Village Boundary
```json
{
  "id": "uuid",
  "villageNo": 1,
  "name": "หนองตุ้ม",
  "boundary": {
    "type": "Polygon",
    "coordinates": [[[lng, lat], [lng, lat], ...]]
  },
  "centerPoint": {
    "type": "Point",
    "coordinates": [lng, lat]
  }
}
```

---

## 🛠️ การแก้ไขปัญหา

### ปัญหา: ไม่สามารถบันทึกขอบเขตได้

**สาเหตุ:**
- ไม่ได้วาดขอบเขตบนแผนที่
- ไม่ได้กรอกชื่อขอบเขต
- ไม่มีสิทธิ์ Admin/Developer

**วิธีแก้:**
1. ตรวจสอบว่าวาดขอบเขตบนแผนที่แล้ว
2. กรอกชื่อขอบเขตให้ครบถ้วน
3. ตรวจสอบสิทธิ์การเข้าถึง

### ปัญหา: ขอบเขตตำบลไม่แสดงบนแผนที่

**สาเหตุ:**
- ยังไม่ได้บันทึกขอบเขตตำบลในฐานข้อมูล
- API ไม่ตอบสนอง

**วิธีแก้:**
1. ตรวจสอบ Network tab ใน DevTools
2. ตรวจสอบว่า Backend API ทำงานปกติ
3. ตรวจสอบข้อมูลในฐานข้อมูล:
   ```sql
   SELECT * FROM geo_boundaries WHERE type = 'tambon';
   ```

### ปัญหา: ไม่สามารถลบขอบเขตได้

**สาเหตุ:**
- ไม่มีสิทธิ์ลบ
- ขอบเขตถูกใช้งานอยู่

**วิธีแก้:**
1. ตรวจสอบสิทธิ์ Admin/Developer
2. ตรวจสอบ Console สำหรับ error message

### ปัญหา: GeoJSON ที่อัปโหลดไม่ถูกต้อง

**สาเหตุ:**
- รูปแบบ GeoJSON ไม่ถูกต้อง
- ไฟล์เสียหาย

**วิธีแก้:**
1. ตรวจสอบรูปแบบ GeoJSON ที่ https://geojson.io
2. ตรวจสอบว่าเป็น Feature, FeatureCollection, Polygon หรือ MultiPolygon
3. ตรวจสอบ coordinates ว่าอยู่ในรูปแบบ [longitude, latitude]

---

## 📝 หมายเหตุ

### พิกัดอ้างอิง
- **ตำบลเวียง**: 19.9169°N, 99.2145°E
- **อำเภอฝาง**: จังหวัดเชียงใหม่
- **Zoom Level**: 13 (เหมาะสำหรับมองเห็นทั้งตำบล)

### Base Layers
- **Street Map**: OpenStreetMap
- **Satellite**: Esri World Imagery
- **Hybrid**: Satellite + Labels

### สิทธิ์การเข้าถึง
- **Admin**: สามารถจัดการขอบเขตทั้งหมด
- **Developer**: สามารถจัดการขอบเขตทั้งหมด
- **ผู้ใช้ทั่วไป**: ดูขอบเขตได้อย่างเดียว

---

## 🎓 แหล่งข้อมูลเพิ่มเติม

- [Leaflet Documentation](https://leafletjs.com/)
- [Leaflet.draw Plugin](https://leaflet.github.io/Leaflet.draw/)
- [GeoJSON Specification](https://geojson.org/)
- [Prisma Documentation](https://www.prisma.io/docs)

---

## 📅 ประวัติการอัปเดต

### Version 2.0 (วันนี้)
- ✅ เพิ่ม API สำหรับจัดการขอบเขตตำบล
- ✅ เพิ่มฟังก์ชันลบขอบเขตหมู่บ้าน
- ✅ ปรับปรุงให้โหลดขอบเขตตำบลจาก API แทน hardcode
- ✅ เชื่อมโยงขอบเขตกับหมู่บ้านในฐานข้อมูล
- ✅ ปรับปรุง UI/UX ให้ใช้งานง่ายขึ้น

### Version 1.0
- เริ่มต้นระบบจัดการขอบเขต
- รองรับการวาดและอัปโหลด GeoJSON
- Georeference Image Tool

---

**พัฒนาโดย:** Guardian Route Development Team  
**ติดต่อ:** support@guardianroute.local  
**License:** MIT
